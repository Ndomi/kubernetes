apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
    name: EKS-Cluster
    region: us-east-1
nodeGroups:
    - name: ng-1
      instanceType: t2.small
      minSize: 1
      maxSize: 4
      desiredCapacity: 2
      ssh:
          publicKeyName: live_keys